# Development environment configuration
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/deliver_dev
    username: postgres
    password: Ea030903*
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      idle-timeout: 30000
      max-lifetime: 600000
      connection-timeout: 30000
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      format_sql: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: deliver.backend.app@gmail.com
    password: ytbkffrdcnkmotkn
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
    protocol: smtp

  flyway:
    enabled: true
    locations: classpath:db/migration
    validate-on-migrate: false
    clean-disabled: false
    clean-on-validation-error: true
    baseline-on-migrate: true
  data:
    redis:
      password: your_redis_password
      port: 6379
      host: localhost

# Logging
logging:
  level:
    com.deliver.backend: INFO
    org.springframework.security: WARN
    org.springframework.web: INFO
    org.flywaydb: DEBUG